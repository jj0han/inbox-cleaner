---
wave: 1
requirements: ["POL-01", "POL-02"]
files_modified: ["src/server/routers/inbox.ts"]
autonomous: true
---

# Plan 08-01: Backend Hardening & Resilience

Hardens the Gmail API router by expanding resilience coverage and fixing the error swallowing bug in suggestions.

## Tasks

<task>
<name>Fix Suggestions Error Swallowing</name>
<read_first>
- src/server/routers/inbox.ts
</read_first>
<action>
In `inboxRouter.getSuggestions`, modify the `catch` block (approx line 331). 
Instead of returning `[]`, re-throw the error or throw a `TRPCError`.
This ensures React Query on the client receives an error state (`isError: true`), triggering the POL-02 error card UI.
</action>
<acceptance_criteria>
- `inboxRouter.getSuggestions` catch block no longer returns `[]` on Gmail API failure.
- `src/server/routers/inbox.ts` passes `npx tsc --noEmit`.
</acceptance_criteria>
</task>

<task>
<name>Expand withRetry Coverage</name>
<read_first>
- src/server/routers/inbox.ts
- src/lib/gmail-retry.ts
</read_first>
<action>
Apply `withRetry` wrapper to ALL remaining Gmail API calls in `src/server/routers/inbox.ts`:
1. `getSummary`: wrap `gmail.users.messages.list`
2. `startCleanupAction`: wrap `gmail.users.messages.list`
3. `executeBatch`: wrap `gmail.users.messages.batchModify`
4. `undoAction`: wrap `gmail.users.settings.filters.delete` and `gmail.users.messages.batchModify`
5. `applySuggestion`: wrap `gmail.users.messages.list`, `gmail.users.messages.batchModify`, `gmail.users.settings.filters.create`, and `gmail.users.settings.filters.list`
6. `unsubscribe`: wrap `gmail.users.messages.get`, `gmail.users.messages.send`, and the `fetch` call (wait, fetch is not Gmail SDK, but we can retry it if it returns 429/503)
7. `bulkUnsubscribe`: wrap the inner `gmail.users.messages.get`, `gmail.users.messages.send`, and `fetch`.

Ensure the correct `this` context or closure for the Gmail API calls.
</action>
<acceptance_criteria>
- `withRetry` is present on at least 15 new call sites across the listed procedures.
- No regression in cleanup, undo, or unsubscribe flows.
</acceptance_criteria>
</task>

## Verification

### Automated
- `pnpm tsc --noEmit` exits 0.

### Manual
1. **POL-02 Fix**:
   - Mock a failure in `getSuggestions` (e.g., throw Error at top of procedure).
   - Verify Dashboard Suggestions section shows the ⚠️ Error Card + Retry button.
2. **Resilience**:
   - Verify cleanup and unsubscribe still work normally.
