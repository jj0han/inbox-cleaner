---
milestone: "1.1"
audited: "2026-05-01T01:36:12Z"
status: "passed"
scores:
  requirements: 9/9
  phases: 3/3
  integration: 2/3
  flows: 3/3
gaps:
  requirements:
    - id: "POL-02"
      status: "partial"
      phase: "07-polish-resilience"
      claimed_by_plans: ["07-02-PLAN.md"]
      completed_by_plans: ["07-SUMMARY.md"]
      verification_status: "passed"
      evidence: "UI wiring is correct, but backend swallows errors (returns []) instead of throwing, so isSuggestionsError is never true. 'Try again' card will not show."
  integration:
    - phase: "07-polish-resilience"
      issue: "Missing resilience coverage for bulk actions."
      description: "withRetry is only applied to getPreview, getSuggestions, and getCardCounts. It is missing from startCleanupAction, executeBatch, applySuggestion, and bulkUnsubscribe, which are high-risk operations."
  verification:
    - phase: "05-real-email-preview"
      issue: "Missing VERIFICATION.md"
      description: "Phase 5 was completed without a structured verification artifact."
    - phase: "06-unsubscribe-link-detection"
      issue: "Unchecked UAT"
      description: "VERIFICATION.md exists but all checkboxes are empty ([ ]), although SUMMARY.md claims completion."
tech_debt:
  - phase: "07-polish-resilience"
    items:
      - "getSuggestions returns [] on error, masking API failures from the client."
      - "withRetry missing from unsubscribe/bulkUnsubscribe."
  - phase: "06-unsubscribe-link-detection"
    items:
      - "mailto: unsubscribe sends raw base64 email; verify character encoding for special subjects."
---

# Milestone v1.1 Audit Report: Real Preview, Polish & Unsubscribe

## 1. Requirements Coverage

| ID | Requirement | Status | Evidence |
| :--- | :--- | :--- | :--- |
| **PREV-01** | Real sender names and subjects in Preview | **Satisfied** | `preview-modal.tsx` renders `email.sender` and `email.subject`. |
| **PREV-02** | Email snippet preview | **Satisfied** | `getPreview` returns 120-char snippet; modal renders it. |
| **PREV-03** | Action-specific preview fetches | **Satisfied** | `getPreview` accepts `type` and uses `PREVIEW_QUERIES`. |
| **POL-01** | API rate limit backoff | **Satisfied** | `withRetry` utility implemented with exponential backoff. |
| **POL-02** | SuggestionsSection error state | **Partial** | UI wired to `isError`, but backend returns `[]` on failure, masking error. |
| **POL-03** | Card count graceful degradation | **Satisfied** | `getCardCounts` uses per-key try/catch; `ActionCard` handles null. |
| **UNSUB-01** | Unsubscribe badge visibility | **Satisfied** | `PreviewModal` shows badge based on `email.unsubscribeUrl`. |
| **UNSUB-02** | Single unsubscribe execution | **Satisfied** | `unsubscribe` mutation handles HTTP and Mailto links. |
| **UNSUB-03** | Bulk Unsubscribe + Archive choice | **Satisfied** | Newsletters card has Switch; `useCleanup` wires to `bulkUnsubscribe`. |

## 2. Integration Check

### Wiring Review
- **Phase 5 -> 6**: `getPreview` correctly provides `unsubscribeUrl` which is the foundation for Phase 6 features. ✓
- **Phase 6 -> Dashboard**: The Newsletters Switch is correctly wired to `useCleanup` which calls `bulkUnsubscribe` in batches. ✓
- **Phase 7 -> 5/6**: `withRetry` was applied to Phase 5's `getPreview`. However, it was **not** applied to Phase 6's `bulkUnsubscribe` or `unsubscribe`, creating a resilience gap. ⚠️

### Flow Verification
- **E2E Preview Flow**: User clicks "Ver E-mails" -> Real data loads -> Unsubscribe badge shows -> Success. ✓
- **E2E Cleanup Flow**: User toggles "Descadastrar" -> Clicks "Limpar" -> `bulkUnsubscribe` runs -> `executeBatch` runs -> Success. ✓
- **E2E Error Flow**: `getSuggestions` fails -> Returns `[]` -> Dashboard shows "Nenhuma sugestão" (Empty state) instead of Error Card. ❌ **BUG FOUND**

## 3. Verification Integrity

- **Phase 5**: **CRITICAL GAP**. No `VERIFICATION.md` exists. Requirements were verified post-hoc during this audit.
- **Phase 6**: **MINOR GAP**. `VERIFICATION.md` is empty (no `[x]`). SUMMARY.md claims success, but UAT trail is missing.
- **Phase 7**: **PASSED**. Full verification trail exists and matches implementation.

## 4. Next Steps Summary

1. **Fix POL-02 Bug**: Update `getSuggestions` to throw error on failure instead of returning `[]`.
2. **Expand Resilience**: Apply `withRetry` to all remaining Gmail procedures.
3. **Formalize Phase 5/6 Verification**: Fill/create the missing artifacts.
